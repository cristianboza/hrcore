# PostgreSQL credentials for docker-compose
POSTGRES_USER=hruser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=hrdb

# When the Spring Boot app runs locally you can connect to the DB on localhost.
# If you run the app in Docker compose alongside the DB, set DB_HOST=db
DB_HOST=localhost

